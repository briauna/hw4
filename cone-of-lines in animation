function setup() { 
createCanvas(400, 400);
	background(255);
	lastMouseX=0
	lastMouseY=0

}


function draw() { 
	var h = random(360)
  stroke(h, 50, 90);
	for ( var x= 10; x<height; x=x+10) {
		
		line(lastMouseX= mouseX,lastMouseY= mouseY,x,height);
	}
}
